## 2026-04-11
- `lsp_diagnostics` cannot validate `.svx` files in this workspace because no LSP server is configured for the extension. Validation had to rely on `npm run check` and `npm run build` instead.
